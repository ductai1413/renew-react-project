import React from 'react';
import type { VideoProps } from '../interfaces/VideoProps';

const Video: React.FC<VideoProps> = () => (
  <div>
    {/* <Thumbnail video={video} />
      <a href={video.url}>
        <h3>{video.title}</h3>
        <p>{video.description}</p>
      </a>
      <LikeButton video={video} /> */}
  </div>
);

export default Video;
